# Direct dependencies - edit THIS file, then regenerate the pinned,
# hash-checked requirements.txt that Render installs (Render builds with
# Python 3.14, so compile for that, not for your local Python):
#
#   uv pip compile requirements.in --universal --python-version 3.14 #       --generate-hashes -o requirements.txt
#   uvx pip-audit -r requirements.txt --require-hashes --disable-pip
#
# Commit both files. Pins only change when someone chooses to change them.
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
langchain>=0.1.16
langchain-openai>=0.1.3
langchain-community>=0.0.34
langchain-groq>=0.1.9
langchain-google-genai>=1.0.10
langgraph>=0.2
sqlglot>=25
python-dotenv>=1.0
tqdm>=4.66
psycopg2-binary>=2.9
pgvector>=0.3.6
fastembed>=0.4.2
